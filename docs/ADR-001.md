# ADR-001 — Core Runtime Architecture

Accepted.

Use Go 1.27.x + Chi, REST + WebSocket events, Asterisk as SIP/RTP media gateway via AudioSocket, PostgreSQL as source of truth, and a first-class Tool Registry.

Direct SIP/RTP implementation in Go is rejected for the MVP.
